const fs=require('fs'); const c=fs.readFileSync('tawjihi/data/_staging/catalog-reconciled.js','utf8'); const fn=new Function('return '+c.match(/const TW_CATALOG\s*=\s*(\[[\s\S]*?\]);/)[1]+';'); const entries=fn(); console.log('Total entries:',entries.length); const ids=entries.map(e=>e.id); const blanks=ids.filter(x=>!x); console.log('Blank IDs:',blanks.length); const esiIds=ids.filter(x=>x&&x.includes('esi')); console.log('ESI ids:',esiIds); const medIds=ids.filter(x=>x&&x.startsWith('med-')); console.log('MED ids:',medIds); const dupes=ids.filter((id,i)=>ids.indexOf(id)!==i); console.log('Dupes:',dupes);